# 微调操作指南 — 楚只管按步骤走

## 你要做的事（3步，总共10分钟操作）

### 第1步：注册 AutoDL（5分钟）

1. 打开 https://www.autodl.com
2. 注册账号
3. 充值 **¥10**（够训练3-5次）
4. 点「创建实例」
5. GPU选 **RTX 4090** 或 **L4**（哪个便宜选哪个，约¥1-2/小时）
6. 镜像选 **PyTorch 2.0 + Python 3.10**
7. 点「创建」，等实例启动（约30秒）

### 第2步：上传2个文件（2分钟）

在 AutoDL 的 JupyterLab 界面，点左上角「上传」按钮，上传这2个文件到 `/root/`：

1. **`training_data_v2.jsonl`** — 训练数据（55条）
2. **`finetune_v2.py`** — 训练脚本

### 第3步：运行训练（3分钟操作，30-60分钟等待）

在 AutoDL 终端里复制粘贴这3行命令：

```bash
cd /root
pip install unsloth transformers datasets trl peft bitsandbytes accelerate
python finetune_v2.py
```

然后等它跑完。看到 "训练完成！" 就行了。

### 第4步：下载模型

在 AutoDL 文件管理器里，下载 `/root/claw_brain_lora_v2/` 文件夹（约100MB）。

---

## 训练完成后怎么验证效果

脚本会自动跑4个测试用例，你在终端里直接看输出：

- 如果模型说"先推理能力再选方向"→ 训练成功
- 如果模型说"帮你做PPT/简历"→ 还需要继续调数据

## 费用

- RTX 4090: ~¥2/小时 × 1小时 = **¥2**
- L4: ~¥1.68/小时 × 1小时 = **¥1.7**

## 如果效果不满意

1. 告诉我"Brain哪里想得不对"
2. 我把你的反馈加进训练数据
3. 重新上传训练数据，再跑一次

每次微调都是在上一版基础上改进，反复迭代直到满意。

## 文件清单

| 文件 | 说明 |
|------|------|
| `training_data_v2.jsonl` | 55条手写高质量训练数据 |
| `finetune_v2.py` | 一键训练脚本 |
| `build_training_v2.py` | 数据集生成脚本（在本地跑，不需要上传） |
